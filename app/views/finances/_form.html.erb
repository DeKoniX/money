<%= simple_form_for @finance do |f| %>
  <% if @finance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@finance.errors.count, "error") %> prohibited this finance from being saved:</h2>

      <ul>
        <% @finance.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%#<%= f.number_field :wallet_id, :value => params[:wallet], :type => "hidden" %>%>
  <%= f.association :wallet, as: :radio_buttons, selected: params[:wallet] %>
  <%= f.input :m, label: 'Сумма: ' %>
  <%= f.input :desc, label: 'Описание: ', as: :string  %>
  <%= f.button :submit %>
<% end %>
